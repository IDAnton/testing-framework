CREATE_EVENTS_TABLE=CREATE TABLE IF NOT EXISTS events (request_id VARCHAR PRIMARY KEY, status VARCHAR)
SELECT_STATUS_WITH_ID=SELECT status FROM events WHERE request_id = ?
CLEAR_EVENTS_TABLE=TRUNCATE TABLE events CASCADE